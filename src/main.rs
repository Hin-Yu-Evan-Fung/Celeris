use sophos::board::fen::TRICKY_FEN;
use sophos::board::*;
use sophos::utils::*;

fn main() {
    perft_bench();
    // let mut board = Board::from_fen(TRICKY_FEN).unwrap();
    // perft_test(&mut board, 6);
}
